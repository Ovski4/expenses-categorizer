<form
    method="post"
    action="{{ path('sub_category_transaction_rule_delete', {'id': sub_category_transaction_rule.id}) }}"
    onsubmit="return confirm('Are you sure you want to delete this item?');"
    class="delete-button"
>
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sub_category_transaction_rule.id) }}">
    <button class="btn btn-outline-danger">Delete this rule</button>
</form>
