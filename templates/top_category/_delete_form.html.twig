<form
    method="post"
    action="{{ path('top_category_delete', {'id': top_category.id}) }}"
    onsubmit="return confirm('{% trans %}Are you sure you want to delete this item?{% endtrans %}');"
    class="delete-button"
>
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ top_category.id) }}">
    <button class="btn btn-outline-danger">{% trans %}Delete this top category{% endtrans %}</button>
</form>
