#!/bin/sh
set -e

# run migrations
php bin/console doctrine:migrations:migrate --no-interaction

# first arg is `-f` or `--some-option`
if [ "${1#-}" != "$1" ]; then
	set -- php-fpm "$@"
fi

exec "$@"
